# INSTANCE_ID of bootstrap MUST "0"
INSTANCE_ID: "0"

# The port on which a node listens for external messages
NODE_PORT: "5776"

# DEBUG_MODE is used to output more logs (true or other)
DEBUG_MODE: "true"

# TEST_MODE is used to output a large number of logs (true or other)
TEST_MODE: "true"

# This node network address
HOST: "bootstrap:5776"

# The waiting time before replying (second)
WAIT_TIME: "10"

# The number of neighbors in the first step
DENSITY: "3"